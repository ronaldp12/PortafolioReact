import reactIcon from '../../../assets/icons/reactIcon.png';

export const ReactIcon = ({estilos}) => {
  return (
    <div>
        <img className={estilos} src={reactIcon} alt="react Icon" />
      
    </div>
  )
}

