import htmlIcon from '../../../assets/icons/html-icon.png'

export const HtmlIcon = ({estilos}) => {
  return (
    <div>
      <img className={estilos} src={htmlIcon} alt="Html Icon" />
    </div>
  )
}
